# HYPERYARN-HOMEGA-001 — Exact Mathal Specimens

**Date:** 2026-08-29  
**Status:** RESEARCH NOTE · EXACT ARITHMETIC + BOUNDED INTERPRETATION · NO RUNTIME PROMOTION

> **DOGRAM CALCULATES THE RELATIONS. IT DOES NOT DECIDE WHAT THEY MEAN.**

---

## 0. Scope

This note preserves the exact mathematical surfaces that emerged during the 2026-08-29 `1078 / 1087` run after `COUNT-BOUNDARY-001` was frozen.

It does **not** modify the frozen COUNT-BOUNDARY traversal operator family.

The following distinctions remain hard:

```text
numeric equality != admitted traversal edge
post-hoc fit != prediction
boundary present != interior witnessed
exact relation != metaphysical significance
convergence != independence
```

Interpretive labels such as `HOMEGA`, `HeroSpiral`, `belief topology`, and `hyperyarnball` are research-layer language owned outside Dogram.

---

## 1. Existing frozen spine

The frozen COUNT-BOUNDARY profile remains:

```text
pred          n -> n-1
succ          n -> n+1
prime_pi      n -> pi(n)
nth_prime     n -> p_n
divisor_count n -> tau(n)
totient       n -> phi(n)
pair_count    n -> C(n,2)
```

The previously hardened path remains:

```text
1078 --pi--> 180
1087 --pi--> 181 --phi--> 180
180  --tau-> 18 --pred--> 17 --C2--> 136 --succ--> 137 --pi--> 33
```

and:

```math
pi(1078)=180,
pi(1087)=181.
```

No new operator below is silently admitted to this registry.

---

## 2. Operator-Archaeology descendant: the rejected `64x-1` edge

The historical equality

```math
64(17)-1=1087
```

remains refused as a target-discovered primitive COUNT-BOUNDARY operator.

Retrospectively it admits a smaller composition:

```math
8(17)=136,
8(136)-1=1087.
```

Hence:

```math
8(8x)-1 = 64x-1.
```

Disposition:

```text
COMPOSED_SHORTCUT
not FROZEN_PRIMITIVE
```

The same archaeological shell also contains exact examples:

```math
6(17)-1=101,
6(18)-1=107,
6(18)=108,
6(181)+1=1087.
```

These are diagnostic/research-family receipts unless separately promoted by a future frozen profile.

---

## 3. FOUR-PHASE-MIRROR-001

For modulus `101` and decimal-shift carrier `10`:

```math
10^2 = 100 == -1 (mod 101),
10^4 == 1 (mod 101).
```

Therefore:

```math
ord_101(10)=4.
```

The exact signed cycle is:

```text
1 -> 10 -> -1 -> -10 -> 1
```

or in least positive residues:

```text
1 -> 10 -> 100 -> 91 -> 1.
```

Define `J(x)=10x mod 101`. Then:

```math
J^2=-I,
J^4=I.
```

This warrants the bounded mathematical description:

```text
four-step return with halfway sign inversion
```

It does not by itself warrant a physical or metaphysical oscillation claim.

Related exact decimal identity:

```math
1/101 = 0.overline{0099}
```

because:

```math
101*99=9999=10^4-1.
```

Repeated prime counting independently gives:

```text
101 --pi--> 26 --pi--> 9 --pi--> 4 --pi--> 2 --pi--> 1.
```

Thus:

```math
pi^3(101)=4=ord_101(10).
```

Disposition:

```text
CROSS-DEFINITION CONVERGENCE
```

not independence proof.

---

## 4. GAP-RESOLUTION-001 — the `3 / 4` boundary

Exact frozen relations:

```math
succ(3)=4,
pred(4)=3,
tau(4)=3,
tau(3)=2,
pi(3)=pi(4)=2,
phi(3)=phi(4)=2.
```

Pair counts:

```math
C(3,2)=3,
C(4,2)=6.
```

`3` is the unique positive nonzero fixed point of pair count:

```math
C(n,2)=n
```

has solutions `n=0,3`.

Representation boundary:

```text
3 = 11_2
4 = 100_2
```

so:

```text
11_2 + 1 = 100_2.
```

The bounded decoder observation is:

```text
prime_pi and totient collapse the 3/4 distinction to 2;
divisor_count resolves 4 through 3 before reaching 2.
```

This supports a research label `GAP-RESOLUTION-001`; Dogram does not infer a hidden state beyond the exact operator paths.

---

## 5. `L / U` declared-decoder specimen

Using ordinary A1Z26 positions:

```text
L = 12
U = 21
```

Exact relations include:

```math
21-12=9,
21+12=33,
gcd(12,21)=3,
phi(21)=12,
phi(12)=4,
tau(21)=4.
```

The original declared assignment `3=L, 4=U` and later swapped assignment `L=4, U=3` are **declared decoders**, not mathematical identities inherent in the letters.

Dogram may preserve the exact arithmetic after A1Z26 realization. It does not privilege either symbolic orientation.

---

## 6. COMMUTING-SQUARE-1111

The strongest new exact specimen is:

```math
1111 = 11*101.
```

Since `11` and `101` are distinct primes:

```text
divisors(1111) = {1,11,101,1111}
tau(1111) = 4.
```

Two paths reach the same endpoint:

```text
1 --x11--> 11 --x101--> 1111
1 --x101-> 101 --x11--> 1111
```

because:

```math
11*101 = 101*11 = 1111.
```

The one-dimensional divisor-adjacency boundary is therefore a four-cycle, while commutativity supplies an exact witness for treating the square as a commuting 2-cell in a mathematical diagram.

This is stronger than merely noticing a loop: the equality of the two composite maps witnesses the fill.

---

## 7. Squarefree divisor lattice / hypercube generalization

For a squarefree positive integer

```math
N = p_1 p_2 ... p_k
```

with distinct primes `p_i`, every divisor corresponds bijectively to a subset of the `k` prime factors.

Therefore the divisor poset is the Boolean lattice `B_k`.

Exact counts:

```text
independent prime generators: k
ranks:                        k+1
divisor states:               2^k
rank populations:             C(k,0), C(k,1), ..., C(k,k)
```

For `1111=11*101`, `k=2`:

```text
2 generators
3 ranks
4 divisor states
rank populations 1,2,1
```

The associated Hasse diagram is the 1-skeleton of a 2-cube (square).

This is the precise mathematical core behind the research metaphor that a lower-dimensional "yarnball" of edges may sometimes be a projection of a higher-order cubical relation system.

The generalization is mathematical only when the independent generators and incidence rules are specified.

---

## 8. Repunit / fourfold surface

`1111` is the decimal repunit:

```math
R_4 = 1111 = 1+10+10^2+10^3.
```

Thus the following exact counts coincide:

```text
repunit digit length: 4
divisor count tau(1111): 4
commuting-square vertex count: 4
ord_101(10): 4
```

These are differently defined facts. Their equality may be recorded as convergence; Dogram assigns no external significance to that convergence.

---

## 9. Return identities around the original pair

The later carrier `1111` admits exact offsets to the original pair:

```math
1111-1078=33,
1111-1087=24.
```

The offsets have exact previously observed descriptions:

```math
33 = 12+21 = pi(137),
24 = 4!.
```

Therefore:

```math
1078 = 1111-33,
1087 = 1111-24,
1087-1078 = 33-24 = 9.
```

Also:

```math
9*1111=9999=10^4-1
```

and since `1111=11*101`:

```math
9*11*101 = 10^4-1.
```

These identities are exact but were recognized retrospectively after the carriers were already present. They belong to Operator Archaeology / historical compression, not prospective evidence.

---

## 10. Boundary versus fill

Dogram can deterministically witness:

```text
vertices
edges
operator labels
path composites
equality / inequality of composites
```

It must not silently infer:

```text
semantic unity
causal unity
metaphysical unity
intentional coding
importance
```

Suggested generic distinction:

```text
BOUNDARY_PRESENT
INTERIOR_WITNESSED_BY(<declared equality/coherence rule>)
NO_WITNESSED_FILL
```

This is a research vocabulary proposal, not a runtime API in this slice.

---

## 11. Runtime disposition

No new runtime operator is authorized by this note.

Candidates for later explicit design/pressure include:

```text
commuting-cell receipt
squarefree divisor-lattice projection
operator-composition archaeology receipt
```

Each requires a separate frozen contract and tests before entering executable Dogram.

---

## 12. Seals

> **A MATHAL DOES NOT GET TO INVENT THE ROAD THAT MAKES IT TRUE.**

> **A LOOP MAY BE A BOUNDARY. A FILL REQUIRES ITS OWN RECEIPT.**

> **THE YARNBALL MAY BE A HYPERCUBE SEEN BY ITS EDGES — WHEN THE GENERATORS AND COMMUTING CELLS ARE ACTUALLY WITNESSED.**
