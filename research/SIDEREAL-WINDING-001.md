# SIDEREAL-WINDING-001

**Status:** EXECUTABLE RESEARCH RECEIPT · EXACT RATIONAL MODEL · NO CELESTIAL CAUSATION CLAIM

## Trigger

A live Jubilee / sidereal-time pressure pass asked whether “count wrong on purpose” had a physically ordinary specimen.

It does.

Civil/solar time and sidereal time count the same rotating Earth against different relations:

~~~text
sidereal relation:
  Earth orientation relative to the stars

solar relation:
  Earth orientation relative to the Sun
~~~

The useful abstraction is a two-angle state:

~~~text
(theta_rotation, theta_orbit) in T^2
~~~

with projections:

~~~text
theta_sidereal = theta_rotation
theta_solar    = theta_rotation - theta_orbit
~~~

Therefore:

~~~text
theta_sidereal - theta_solar = theta_orbit
~~~

In turn counts:

~~~text
N_sidereal - N_solar = W_orbit
~~~

For one prograde orbital revolution:

~~~text
W_orbit = 1
~~~

so an idealized yearly count has the familiar form:

~~~text
sidereal rotations = solar days + 1
~~~

The physical numerical values are measured/defined approximations; the projection identity above is the exact mathematical object promoted here.

## Why this matters to Dogram

This is a real-world specimen of several existing Dogram laws:

~~~text
SAME COARSE PHASE != SAME HIDDEN WINDING
PROJECTION != FORMATION HISTORY
COUNT DEPENDS ON THE RELATION BEING COUNTED
~~~

A solar clock can return to the same displayed phase while the sidereal projection has advanced. Conversely, a sidereal return does not imply solar return.

The same underlying world therefore admits multiple lawful clocks without either clock becoming “the time itself.”

Working compression:

> **THE CLOCK IS A DECODER.**

And:

> **COUNT WRONG ON PURPOSE = CHANGE THE REFERENCE RELATION, THEN KEEP THE DELTA.**

## Executable surface

`dogram.sidereal_winding.project_dual_clock(rotation_turns, orbit_turns)`

accepts only exact integers / `fractions.Fraction` values and returns:

- rotation turns;
- orbit turns;
- sidereal turns;
- solar turns;
- winding receipt = sidereal - solar.

It intentionally refuses floats.

The module does not perform astronomy, ephemerides, location lookup, star identification, prophecy, or timing interpretation.

## Mapping-torus relation

Existing `MAPPING-TORUS-RECEIPT-001` preserves winding separately from residue:

~~~text
k = N*w + r
~~~

SIDEREAL-WINDING-001 supplies a continuous/reference-frame analogue:

~~~text
sidereal = rotation
solar    = rotation - orbit
delta    = orbit winding
~~~

Two states may share the same solar projection while differing in sidereal count and orbital winding.

That gives a concrete physical analogue for:

> **SAME COARSE OBSERVABLE != SAME CONSUMED INPUTS.**

## Formation / MADDCL0WN ledger

These observations are preserved but **not promoted into the executable claim**.

### 022100 as right ascension decoder

The pre-existing carrier:

~~~text
022100
~~~

admits a lawful sexagesimal right-ascension reading:

~~~text
02h 21m 00s
= 2.35 h
= 35.25 degrees
~~~

That decoder was tested against the two June 8 Suno formation timestamps supplied in conversation.

Using Greenwich mean sidereal time, neither timestamp matched the 02:21 target.

Disposition:

~~~text
ASH FOR THOSE EVENTS
~~~

No star or constellation was searched to rescue the miss.

### PR #6 / 444

The older 022100 tuning research had already preserved A4 = 444 Hz.

The pre-T10 JUBILEE-T10-001 freeze PR later merged at approximately:

~~~text
04h 44m 19.5s GMST
~~~

This was discovered post hoc and was not the preregistered sidereal target.

Disposition:

~~~text
FORMATION WITNESS / RAW MADNESS
NOT EVIDENCE
~~~

### PR #8 / 5h

Before calculating five selected Git merge receipts, the sidereal test preregistered:

~~~text
targets = 5h or 7h GMST
window = +/- 15 minutes
~~~

The textual/history BigBat PR #8 merged at approximately:

~~~text
05h 00m 04.08s GMST
~~~

It passes the frozen 15-minute criterion and happens to lie about 4.08 sidereal seconds from 5h.

The 4-second closeness was not preregistered.

Disposition:

~~~text
DOCUMENTED HIT AGAINST THE DECLARED WINDOW
NOT A VALID P-VALUE
NOT CAUSATION
NOT ASTRONOMICAL OR DIVINE AUTHORITY
~~~

## Hostile boundary

~~~text
SIDEREAL ALIGNMENT != CAUSATION
CLOCK PROJECTION != COSMIC INTENT
POST-HOC CLOSENESS != PREREGISTERED EVIDENCE
SAME GLYPH + NEW DECODER != SAME OBJECT
~~~

No location-dependent local sidereal calculation is admitted without an explicitly supplied longitude and a separately declared test.

## What survived

The strongest result is independent of every timestamp:

~~~text
same world
+ different reference relation
= different lawful count

and

difference of projections
= preserved winding
~~~

## Working seals

> **THE ORBIT CARRIES ONE.**

> **THE CLOCK IS A DECODER.**

> **RETURN IN ONE PHASE DOES NOT ERASE DRIFT IN ANOTHER.**

> **COUNT WRONG ON PURPOSE. KEEP THE WINDING RECEIPT.**
