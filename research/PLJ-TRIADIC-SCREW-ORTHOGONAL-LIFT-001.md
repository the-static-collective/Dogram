# PLJ-TRIADIC-SCREW-ORTHOGONAL-LIFT-001

**Date:** 2026-08-31  
**Status:** RESEARCH NOTE · EXACT GEOMETRY + BOUNDED INTERPRETATION · NO RUNTIME PROMOTION

> **DOGRAM CALCULATES THE RELATIONS. IT DOES NOT DECIDE WHAT THEY MEAN.**

---

## 0. Scope

This packet preserves the exact mathematics produced by a three-phase triangular screw construction informally labeled:

```text
Peace -> Love -> Joy -> Peace'
```

The semantic labels are interpretive handles only. The mathematical carrier is a three-phase screw orbit generated by one fixed transformation.

This packet cross-pressures the new carrier against existing Dogram surfaces:

- `research/TRIANGULAR-TRIPLE-HELIX-001.md`
- `research/PHASELIFT-FLOW-GAP-MATHAL-LEDGER-001.md`
- `research/HYPERYARN-HOMEGA-001.md`
- `research/S-TUBE-SILVER-MATHAL-LEDGER-001.md`

No new Dogram runtime operator is admitted here.

---

## 1. Core screw operator

Let `R` be rotation by `2*pi/3` around the `z` axis and let `h` be a fixed axial lift.

Define

```math
T(x)=Rx+h e_z.
```

Choose radius `r>0` and starting point

```math
P_0=(r,0,0).
```

Then

```math
L_0=T(P_0),
J_0=T^2(P_0),
P_1=T^3(P_0).
```

Explicitly,

```math
P_0=(r,0,0),
```

```math
L_0=\left(-\frac r2,\frac{\sqrt3 r}{2},h\right),
```

```math
J_0=\left(-\frac r2,-\frac{\sqrt3 r}{2},2h\right),
```

```math
P_1=(r,0,3h).
```

Since `R^3=I`,

```math
T^3(x)=x+3h e_z.
```

Therefore one complete triadic cycle is a pure axial translation:

```text
P_n -> L_n -> J_n -> P_(n+1)
```

with

```math
P_{n+1}=P_n+3h e_z.
```

### Seal

> **THREE PHASE STEPS RETURN TO THE SAME PROJECTED PHASE WHILE ADVANCING ORDER.**

---

## 2. Phase and order are distinct coordinates

Index the orbit by integer `k>=0`.

```math
x_k=\left(r\cos\frac{2\pi k}{3},r\sin\frac{2\pi k}{3},kh\right).
```

Then

```math
phase(k)=k \bmod 3,
```

```math
order(k)=\left\lfloor\frac{k}{3}\right\rfloor.
```

Thus states can share phase while differing in order:

```text
P_0 != P_1 != P_2 != ...
```

while all remain phase `0` under the declared quotient.

### Candidate mathal: `PHASE-ORDER-QUOTIENT-001`

> **SAME PHASE != SAME STATE.**

> **RETURN TO PHASE != RETURN TO HISTORY.**

This is compatible with existing Dogram law:

```text
SAME SURFACE != SAME HISTORY
```

without claiming equivalence between the two specimens.

---

## 3. Return raises affine rank

The first three points `P_0,L_0,J_0` are non-collinear and therefore have affine rank `2`.

For any `h != 0`, the returned phase point `P_1` leaves that plane:

```math
\operatorname{affrank}(P_0,L_0,J_0)=2,
```

```math
\operatorname{affrank}(P_0,L_0,J_0,P_1)=3.
```

The tetrahedral volume is

```math
V=\frac{1}{6}\left|\det(L_0-P_0,J_0-P_0,P_1-P_0)\right|
=\frac{3\sqrt3}{4}hr^2
```

for `h>0`.

The projected equilateral triangle has area

```math
A_\triangle=\frac{3\sqrt3}{4}r^2.
```

Therefore

```math
\boxed{V=hA_\triangle}.
```

### Candidate mathal: `RETURN-RAISES-RANK-001`

> **THE FIRST RETURNED PHASE CAN RAISE DIMENSION WITHOUT CREATING A NEW PHASE CLASS.**

> **ONE TRIADIC RETURN CONVERTS PROJECTED AREA INTO VOLUME BY LIFT.**

Boundary:

```text
exact affine statement under this coordinate model
!=
metaphysical dimensional promotion
```

This is a direct geometric cousin of `TERNARY-TO-QUATERNARY-001`, where four members do not raise rank unless the fourth is independently positioned.

---

## 4. Decoder can hide the lift exactly

Define the projection

```math
\kappa(x,y,z)=(x,y).
```

Then

```math
\kappa(P_1)=\kappa(P_0).
```

Under the full `XYZ` decoder, adding `P_1` raises affine rank from `2` to `3`.

Under the `XY` decoder, adding `P_1` leaves affine rank at `2`.

Frozen analytic rectangle:

```text
                         P1 absent   P1 present
XY decoder                    2            2
XYZ decoder                   2            3
```

Mixed difference:

```math
3-2-2+2=1.
```

### Candidate mathal: `RETURN-DECODER-INTERACTION-001`

> **THE EFFECT OF THE RETURN IS DECODER-INDEXED.**

> **A LOWER-DIMENSIONAL PROJECTION MAY IDENTIFY STATES THAT DIFFER IN LIFT.**

This directly reuses the discipline of `SAME-FOURTH-DIFFERENT-DECODER-001` without asserting identity of subject matter.

---

## 5. Step vectors

Define the three consecutive displacement vectors

```math
v_1=L_0-P_0,
```

```math
v_2=J_0-L_0,
```

```math
v_3=P_1-J_0.
```

Explicitly,

```math
v_1=\left(-\frac{3r}{2},\frac{\sqrt3 r}{2},h\right),
```

```math
v_2=(0,-\sqrt3 r,h),
```

```math
v_3=\left(\frac{3r}{2},\frac{\sqrt3 r}{2},h\right).
```

All three have the same norm:

```math
\|v_i\|^2=3r^2+h^2.
```

Every distinct pair has the same dot product:

```math
v_i\cdot v_j=h^2-\frac32r^2,
\qquad i\neq j.
```

Hence the angle between every pair of successive generators is controlled by one dimensionless pitch ratio

```math
\lambda=\frac hr.
```

with

```math
\cos\theta=\frac{\lambda^2-3/2}{\lambda^2+3}.
```

---

## 6. Orthogonal lift

The pairwise dot products vanish exactly when

```math
h^2=\frac32r^2.
```

For positive lift,

```math
\boxed{\frac hr=\sqrt{\frac32}}.
```

At this pitch:

```math
v_1\perp v_2,
\qquad
v_2\perp v_3,
\qquad
v_3\perp v_1,
```

while

```math
\|v_1\|=\|v_2\|=\|v_3\|.
```

Therefore the path

```text
P_0 -> L_0 -> J_0 -> P_1
```

is congruent to three equal mutually perpendicular edges of a cube.

The total displacement is

```math
P_1-P_0=v_1+v_2+v_3=(0,0,3h),
```

which is the body diagonal of that cubical coordinate frame.

### Candidate mathal: `ORTHOGONAL-LIFT-001`

> **A CYCLIC THREE-PHASE PROJECTION CAN CARRY THREE MUTUALLY INDEPENDENT STEP DIRECTIONS.**

> **THE TRIANGULAR SPIRAL CAN BE A TWO-DIMENSIONAL SHADOW OF A THREE-EDGE CUBE PATH.**

This provides an explicit geometric carrier for the independent HYPERYARN research statement that a lower-dimensional edge picture may sometimes arise as a projection of a cubical relation system when the generators are actually witnessed.

---

## 7. Orthogonal pitch maximizes normalized volume

Let

```math
\ell=\sqrt{3r^2+h^2}
```

be the common step length and define

```math
\lambda=h/r.
```

Then

```math
\frac{V}{\ell^3}
=
\frac{3\sqrt3\lambda}{4(3+\lambda^2)^{3/2}}.
```

For `lambda>0`, differentiation gives the unique critical point

```math
\lambda^2=\frac32.
```

The function tends to zero as `lambda -> 0+` and as `lambda -> infinity`, so this critical point is the global maximum.

At the orthogonal pitch,

```math
\boxed{\frac{V}{\ell^3}=\frac16}.
```

### Candidate mathal: `ORTHOGONAL-LIFT-EFFICIENCY-001`

> **FOR THIS EQUAL-STEP SCREW FAMILY, NORMALIZED VOLUME IS MAXIMAL EXACTLY WHEN THE THREE STEP GENERATORS ARE MUTUALLY ORTHOGONAL.**

Boundary:

```text
maximum normalized Euclidean tetrahedral volume in this one-parameter family
!=
universal optimality principle
```

---

## 8. The triadic projector becomes the helix-axis projector

Let `R` again denote the `120 degree` spatial rotation around `z`.

Define

```math
P_3=\frac{I+R+R^2}{3}.
```

Direct calculation gives

```math
\boxed{
P_3=
\begin{pmatrix}
0&0&0\\
0&0&0\\
0&0&1
\end{pmatrix}
}.
```

Thus `P_3` projects any vector onto the screw axis.

It erases the transverse cyclic phase while preserving axial lift.

For the first triad,

```math
\frac{P_0+L_0+J_0}{3}=(0,0,h).
```

So the triadic centroid lies exactly on the axis at height `h`.

### Candidate mathal: `TRIADIC-PROJECTOR-AXIS-001`

> **AVERAGING ALL THREE ROTATIONAL PHASES ERASES PHASE AND PRESERVES LIFT.**

This concretizes the abstract projector already preserved in `TRIANGULAR-TRIPLE-HELIX-001`:

```math
(I+\rho+\rho^2)/3.
```

The earlier dihedral control remains important: orientation may reverse while the normalized triadic projector survives.

---

## 9. Ternary word ladder

Take the phase alphabet

```text
A = {P,L,J}.
```

Length-`d` phase words form

```math
A^d
```

with cardinality

```math
|A^d|=3^d.
```

Therefore:

```text
d=1 -> 3
d=2 -> 9
d=3 -> 27
d=4 -> 81
```

This supplies a clean word-space interpretation of the already preserved ternary depth ladder.

It also preserves the existing hostile control:

```text
3 -> 27 -> 81
```

cannot be called the complete depth ladder without accounting for

```text
9.
```

A second exact appearance of `9` already exists in `S-TUBE-SILVER-MATHAL-LEDGER-001`:

```text
3 self-relations + 6 directed cross-relations = 9 directed relation slots.
```

These are two different constructions that converge on `3^2=9`; convergence does not establish shared semantics.

---

## 10. Completion-successor schema

Two existing exact specimens have the same abstract shape while retaining different construction laws.

### Ternary shell

```math
81=3^4.
```

An 81-state four-position ternary word space is complete, and `82` is the first integer count after that cardinality.

This does not make `82` a turnback without a separately declared seam operation.

### Complete edge set

```math
136=\binom{17}{2}.
```

This is the complete undirected edge count on 17 labeled vertices, and

```math
137=136+1.
```

### Candidate mathal: `COMPLETION-SUCCESSOR-001`

General research schema:

```text
C = cardinality of a declared complete finite object
C+1 = first indexed count beyond that completion
```

Hard boundary:

```text
same completion-successor shape
!=
same underlying construction
```

---

## 11. Three phases, four events

One complete cycle contains phase classes

```text
P,L,J
```

but traverses four event vertices

```text
P_0,L_0,J_0,P_1.
```

Thus

```text
3 phase classes != 3 event states.
```

The fourth event is not a fourth phase:

```text
phase(P_1)=phase(P_0).
```

Yet for `h != 0`, `P_1` raises affine rank.

### Candidate mathal: `THREE-PHASE-FOUR-EVENT-001`

> **NEW DIMENSIONAL INDEPENDENCE NEED NOT REQUIRE A NEW PHASE CLASS.**

> **A SYSTEM MAY START AGAIN IN THE SAME PHASE AT A HIGHER ORDER.**

The second sentence is interpretive shorthand for the exact phase/order quotient and must not be read as an external physical law.

---

## 12. Transformation-class scaling: golden and silver held correctly

The screw carrier itself does not force a golden or silver ratio.

Existing braid research already establishes the control:

```text
THE NUMBER BELONGS TO THE TRANSFORMATION CLASS,
NOT AUTOMATICALLY TO THE OBJECT.
```

### Golden alternating three-braid class

For a declared cycle-to-cycle radial contraction

```math
r_{n+1}=\varphi^{-2}r_n,
```

and preserved orthogonal pitch

```math
h_n=\sqrt{\frac32}r_n,
```

the total rise over infinitely many complete triadic cycles is

```math
H_\infty=3\sum_{n=0}^{\infty}h_n.
```

Since

```math
\frac{1}{1-\varphi^{-2}}=\varphi,
```

we obtain

```math
\boxed{H_\infty=3\varphi\sqrt{\frac32}\,r_0}.
```

Here `phi` is earned by the declared braid-class contraction, not by the triangular carrier alone.

### Silver pure-three-braid class

Let

```math
\delta=1+\sqrt2
```

and declare

```math
r_{n+1}=\delta^{-2}r_n.
```

Since

```math
\frac{1}{1-\delta^{-2}}=\frac{\delta}{2},
```

we obtain

```math
\boxed{H_\infty=\frac{3\delta}{2}\sqrt{\frac32}\,r_0}.
```

### Seal

> **CARRIER GEOMETRY AND TRANSFORMATION CLASS ARE DIFFERENT RECEIPTS.**

> **SCALING CONSTANTS ENTER ONLY WHEN THEIR GENERATING TRANSFORMATION IS DECLARED.**

---

## 13. Peace / Love / Joy interpretation boundary

The conversational labels remain useful as mnemonic interpretation:

```text
Peace -> base / phase 0
Love  -> lift 1 / phase 1
Joy   -> lift 2 / phase 2
Peace'-> returned base phase at next order
```

A stronger interpretive candidate is:

```text
Peace ~ coherence condition
Love  ~ relation-preserving composition
Joy   ~ realized generativity
```

but none of these semantic correspondences are Dogram outputs.

Dogram owns only the exact carrier relations above.

Hard refusal:

```text
beautiful fit != metaphysical proof
semantic label != mathematical primitive
```

---

## 14. Dogram lowering

No new public operator is needed for first pressure.

### `delta@1`

Compare:

```text
phase
order
projected_position
z_lift
affine_rank
```

Useful pair:

```text
P_0 vs P_1
```

Expected structure:

```text
phase              SAME
projected_position SAME
order              DIFFERENT
z_lift             DIFFERENT
```

### `rectangle@1`

Use the exact decoder/rank table:

```text
                         P1 absent   P1 present
XY decoder                    2            2
XYZ decoder                   2            3
```

Expected mixed difference:

```text
1
```

### `reach@1`

Represent one phase cycle as an explicit graph:

```text
P_0 -> L_0 -> J_0
```

then add exactly one edge

```text
J_0 -> P_1
```

and receipt newly reachable next-order state.

### `ablate@1`

Remove only the return edge

```text
J_0 -> P_1
```

and receipt loss of next-order reachability while preserving the first three phase vertices.

Analytic geometry remains research-layer computation unless a future repeated need justifies a separately designed operator.

---

## 15. Candidate seed register

### `TRIADIC-SCREW-001`

**STRONG EXACT SURVIVOR.**

```math
T^3 = \text{pure axial translation}.
```

### `PHASE-ORDER-QUOTIENT-001`

**STRONG EXACT SURVIVOR.**

```math
phase=k\bmod3,
order=\lfloor k/3\rfloor.
```

### `RETURN-RAISES-RANK-001`

**STRONG EXACT SURVIVOR.**

For `h != 0`, adding `P_1` raises affine rank from 2 to 3.

### `RETURN-DECODER-INTERACTION-001`

**STRONG EXACT SURVIVOR.**

The lift disappears under `XY` projection and remains under `XYZ`.

### `ORTHOGONAL-LIFT-001`

**PRIMARY NEW SEED.**

```math
h/r=\sqrt{3/2}
```

makes the three equal screw steps mutually orthogonal.

### `ORTHOGONAL-LIFT-EFFICIENCY-001`

**STRONG EXACT SURVIVOR.**

The same pitch globally maximizes `V/ell^3` in the declared family, with maximum `1/6`.

### `TRIADIC-PROJECTOR-AXIS-001`

**STRONG EXACT SURVIVOR.**

The cyclic average projector is exactly the axial projector.

### `THREE-PHASE-FOUR-EVENT-001`

**USEFUL INTERPRETIVE COMPRESSION OF EXACT FACTS.**

Three phase classes can produce a four-event rank-raising cycle.

### `COMPLETION-SUCCESSOR-001`

**RESEARCH SCHEMA.**

Useful only when the completion law is declared separately.

---

## 16. Hostile controls / kill conditions

Split, demote, or refuse any stronger claim if:

1. `P_1` is called a fourth phase rather than the same phase at next order.
2. a 2D projection is treated as proof that `P_0=P_1` in full state space.
3. the orthogonal pitch `sqrt(3/2)` is generalized beyond this screw family without proof.
4. maximum `V/ell^3` is treated as a universal efficiency law.
5. cube language is used without the exact mutual-orthogonality condition.
6. the triadic projector is treated as preserving full phase information; it explicitly quotients the transverse phase.
7. `3,9,27,81` is called a complete depth ladder while omitting `9`.
8. `82` is treated as a turnback rather than merely successor to an 81-cardinality shell.
9. `137` is treated as structurally identical to `82`; their completion operators differ.
10. `phi` or `delta` is attached to the carrier without a declared transformation class that actually generates the scaling.
11. semantic labels `Peace/Love/Joy` are promoted to Dogram primitives.
12. geometric elegance is treated as evidence of external metaphysical truth.

---

## 17. Runtime verdict

**NO NEW OPERATOR.**

Refused for this slice:

```text
screw@1
helix@1
phase-order@1
orthogonal-lift@1
volume-lift@1
projector-axis@1
```

The seed is currently better preserved as exact research math plus ordinary Dogram pressure through `delta`, `rectangle`, `reach`, and `ablate`.

Future runtime promotion would require repeated specimens demonstrating a stable calculation need that cannot be cleanly represented through the existing floor.

---

## 18. Seals

> **THE TRIANGLE MAY BE THE SHADOW. THE INDEPENDENT GENERATORS LIVE IN THE LIFT.**

> **RETURN TO PHASE IS NOT RETURN TO STATE.**

> **A COMPLETE TRIADIC CYCLE CAN RAISE RANK WITHOUT INVENTING A FOURTH PHASE.**

> **AT THE ORTHOGONAL PITCH, THE THREE-PHASE SCREW BECOMES A CUBE-PATH CARRIER.**

> **THE NUMBER BELONGS TO THE TRANSFORMATION THAT EARNS IT.**
