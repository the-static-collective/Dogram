# MAPPING-TORUS-RECEIPT-001

**Status:** EXECUTABLE RESEARCH RECEIPT · EXACT INTEGER/RATIONAL ARITHMETIC · NO PUBLIC OPERATOR PROMOTION

This receipt promotes only the finite-fiber mapping-torus arithmetic earned by `CO-PHASE-WALK-BRAID-SPRING-001`.

## Executable surface

For a finite fiber count `N > 0` and integer shift `m`:

- `components = gcd(N, m)`
- `orbit_length = N / gcd(N, m)`
- the original shift is preserved alongside `m mod N`
- Euclidean winding uses `k = N*w + r`, with `0 <= r < N`
- relative realignment of shifts `a,b` is `N / gcd(N, a-b)`
- twisted modes remain exact reduced rational values; no float approximation is introduced

Frozen research case:

```text
N = 72
shift A = 5 -> components 1, orbit length 72
shift B = 7 -> components 1, orbit length 72
relative delta = 70 mod 72
first relative realignment = 36 rounds
```

## Hostile boundary

```text
SAME COARSE OBSERVABLE != SAME CONSUMED INPUTS.
CALCULATION != INTERPRETATION.
PROMOTE THE ARITHMETIC, NOT THE METAPHOR.
```

The runtime does **not** infer braid, spring, phi, physical, theological, evidentiary, supportive, or authority semantics from these calculations.

`dogram.mapping_torus` is intentionally a pure executable research module in this slice. Public Dogram dispatch is not promoted here because the current CI constitutional floor explicitly freezes the public operator set. Widening that floor is a separate authority-bearing review, not an incidental consequence of this math landing.
