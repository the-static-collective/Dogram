# PHASELIFT-FLOW-GAP-MATHAL-LEDGER-001 — Fourthness, Decoder Horizon, Flow, Gap, and Bridge Birth

**Date:** 2026-08-30  
**Status:** RESEARCH + FROZEN EXECUTABLE WITNESSES · NO NEW RUNTIME OPERATOR  
**Owner boundary:** Dogram calculates declared differences, interactions, ablations, and reachability. It does not decide phenomenology, metaphysics, theology, causation, truth, evidence, value, or authority.

> **DO THE MATH. SHOW THE DELTA. KEEP THE RECEIPT. DO NOT DECIDE WHAT IT MEANS.**

## 0. Why this packet exists

A live PHASELIFT / Dogram conversation generated a connected family of mathals around:

- ternary -> quaternary transition;
- fourthness as a typed relation rather than one scalar attribute;
- source authority vs receiver-local authorization;
- birth of a future edge;
- HOME as a declared continuity relation;
- same carrier under different decoders;
- singular decoder horizons and path-only information;
- endpoint equivalence vs approach-history equivalence;
- flow alignment vs a missing crossing;
- synchrony / nearest approach vs actual connectivity;
- bridge birth as a topology-changing event.

The first executable pass deliberately uses only the existing public Phase A floor:

```text
delta@1
rectangle@1
ablate@1
reach@1
```

Existing Dogram Ω receipts are reused where they already prove the needed execution distinction. No new public operator is admitted.

## 1. Receipt classes

This packet keeps four classes separate:

```text
FROZEN EXECUTABLE
  -> committed fixture + unit test against current Dogram operator semantics

EXISTING OMEGA RECEIPT
  -> already proved by the landed Ω cycle and its tests

EXACT ANALYTIC
  -> ordinary mathematics calculated outside a new Dogram operator

HOLD / MODEL
  -> useful formulation not promoted to runtime or physical/metaphysical claim
```

Frozen suite:

```text
tests/test_phaselift_flow_gap_mathals.py

tests/fixtures/phaselift_flow/
  ternary-to-quaternary.json
  authority-transfer-rectangle.json
  threshold-birth.json
  home-ablation.json
  same-fourth-different-decoder.json
  one-point-two-histories.json
  flow-has-next.json
  alignment-without-contact.json
  synchrony-bridge-rectangle.json
  bridge-birth.json
```

## 2. `TERNARY-TO-QUATERNARY-001` — FROZEN EXECUTABLE

Choose declared points:

```text
A = (0,0,0)
B = (1,0,0)
C = (0,1,0)
D0 = (1/3,1/3,0)
D1 = (1/3,1/3,1)
```

Exact affine facts:

```text
affrank(A,B,C,D0) = 2
affrank(A,B,C,D1) = 3

tetra_volume(D0) = 0
tetra_volume(D1) = 1/6
```

The frozen `delta@1` surface orders:

```text
member_count
affine_rank
tetra_volume
independent_fourth
```

Expected receipt:

```text
member_count       SAME       4 -> 4
affine_rank        DIFFERENT  2 -> 3   <- first difference
tetra_volume       DIFFERENT  0 -> 1/6
independent_fourth DIFFERENT  false -> true
```

Mathal:

> **FOUR MEMBERS CAN EXIST BEFORE FOURTHNESS RAISES THE RANK.**

Boundary:

```text
rank change under this declared coordinate specimen
!=
proof of a metaphysical dimensional transition
```

## 3. `AUTHORITY-TRANSFER-RECTANGLE-001` — FROZEN EXECUTABLE

Axes:

```text
A = source authority absent / present
B = receiver-local authorization refused / granted
cell = effect executable, encoded 0/1
```

Lawful PHASELIFT rectangle:

```text
      B0 B1
A0     0  1
A1     0  1
```

Exact mixed difference:

```math
1 - 0 - 1 + 0 = 0
```

Expected receipt:

```text
mixed_delta = 0
interaction_detected = false
```

Hostile silent-authority-transfer control:

```text
      B0 B1
A0     0  1
A1     1  1
```

Exact mixed difference:

```math
1 - 1 - 1 + 0 = -1
```

Expected receipt:

```text
mixed_delta = -1
interaction_detected = true
```

Mathal:

> **SOURCE AUTHORITY CONTRIBUTES ZERO INTERACTION TO RECEIVER-LOCAL AUTHORIZATION IN THE LAWFUL SPECIMEN.**

The hostile rectangle is a canary for silent authority expansion. It does not itself decide whether an external system is lawful.

## 4. `THRESHOLD-BIRTH-REACH-001` — FROZEN EXECUTABLE

Before graph:

```text
H -> THETA
W_PRIME -> WORK
```

No edge:

```text
THETA -> W_PRIME
```

Before:

```text
H -/-> W_PRIME
H -/-> WORK
```

One explicit `reach@1` mutation:

```text
ADD_EDGE THETA -> W_PRIME
```

After:

```text
H -> THETA -> W_PRIME
H -> THETA -> W_PRIME -> WORK
```

Mathal:

> **THE POSSIBLE WORLD DOES NOT BECOME A REACHABLE FUTURE IN THIS DECLARED GRAPH UNTIL THE BIRTH EDGE IS ADMITTED.**

Hard refusal:

```text
GRAPH REACHABILITY != HISTORICAL OCCURRENCE
GRAPH PATH != CAUSAL PATH
```

## 5. `HOME-ABLATION-001` — FROZEN EXECUTABLE

Declared continuity-proof graph:

```text
H -> HOME -> THETA -> W_PRIME -> NEXT
```

Ablate only the `HOME` node.

Expected requested-target receipt:

```text
H -> W_PRIME  true -> false
H -> NEXT     true -> false
```

Mathal:

> **HOME IS THE MINIMUM DECLARED RELATION WITHOUT WHICH THIS PARTICULAR CONTINUATION NO LONGER COUNTS AS ATTRIBUTABLE.**

Boundary:

```text
necessary in this declared proof graph
!=
universally or metaphysically necessary
```

## 6. `FOURTHNESS-LATTICE-001` — REFINED BY OMEGA

Initial candidate vector:

```math
F(D) = (P,I,L,H)
```

with:

```text
P = present / countable
I = independent under the declared relation
L = load-bearing for the selected output/path
H = history-bearing
```

A naive Boolean treatment with implications such as:

```text
I -> P
H -> P
L -> H
```

suggests a small state lattice rather than one scalar "fourthness" value.

But the landed Ω cycle forces a stronger correction.

The positive Ω fixture has a diagnostic step that:

```text
is present
executes
consumes diagnostic input
is not referenced by the selected final result
can be removed by the bounded remove_step proposal
leaves the selected result equivalent
changes execution digest / trace / fuel / consumed-input provenance
```

Therefore:

> **RESULT EQUIVALENCE != EXECUTION EQUIVALENCE.**

and:

> **A THING CAN FAIL TO CHANGE THE SELECTED RESULT WHILE STILL CHANGING THE HISTORY THAT PRODUCED IT.**

`H` is therefore too compressed. A minimum research refinement is:

```math
H = (H_E, H_T, H_C)
```

where:

```text
H_E = envelope-bearing
H_T = execution-trace-bearing
H_C = consumption-bearing
```

Dogram's reified execution includes the digest of the supplied input envelope while separately recording `step_trace` and `consumed_input_addresses`. Thus:

> **EXECUTION DIGEST CHANGED != THIS THING WAS CONSUMED.**

and the standing law remains:

> **PRESENCE IS NOT CONSUMPTION.**

The stronger index is not simply `F(D)`, but something like:

```math
F(D | S,Q,C)
```

for declared specimen/system `S`, selected question/output `Q`, and observation/provenance cut `C`.

Mathal:

> **FOURTHNESS IS A RELATION, NOT AN ATTRIBUTE.**

## 7. `SAME-FOURTH-DIFFERENT-DECODER-001` — FROZEN EXECUTABLE + EXACT ANALYTIC

Hold the carrier fixed:

```text
A = (1,0,0,0)
B = (0,1,0,0)
C = (0,0,1,0)
D = (1,1,1,1)
```

Decoder that suppresses the fourth coordinate:

```math
kappa_0(x1,x2,x3,x4) = (x1,x2,x3)
```

Decoder that preserves it:

```math
kappa_1(x1,x2,x3,x4) = (x1,x2,x3,x4)
```

Then:

```text
rank(kappa_0(A,B,C))   = 3
rank(kappa_0(A,B,C,D)) = 3

rank(kappa_1(A,B,C))   = 3
rank(kappa_1(A,B,C,D)) = 4
```

Important correction:

```text
ordinary invertible change of basis cannot change rank
```

so this is a decoder/projection distinction, not merely a basis change.

Frozen `rectangle@1` rank table:

```text
                         decoder collapsed  decoder exposes x4
D absent                         3                  3
D present                        3                  4
```

Mixed difference:

```math
4 - 3 - 3 + 3 = 1
```

Expected receipt:

```text
mixed_delta = 1
interaction_detected = true
```

Mathals:

> **THE EFFECT OF THE FOURTH DEPENDS ON THE DECODER.**

> **INDEPENDENCE IS DECODER-INDEXED.**

A lawful notation is therefore closer to:

```math
I(D | A,B,C; kappa)
```

than intrinsic `I(D)`.

## 8. `DECODER-HORIZON-001` — EXACT ANALYTIC / HOLD FROM RUNTIME

Define the continuous decoder family:

```math
kappa_lambda(x1,x2,x3,x4) = (x1,x2,x3, lambda*x4)
```

For the held carrier above:

```text
rank = 3 when lambda = 0
rank = 4 when lambda != 0
```

So raw decoded presentation can converge continuously while the rank invariant is discontinuous at the exact horizon.

For the fourth-direction residual:

```math
rho_lambda(D) = (0,0,0,lambda)
```

its magnitude tends to zero:

```math
||rho_lambda|| -> 0
```

but the normalized oriented residual is:

```math
u_lambda = rho_lambda / ||rho_lambda|| = sign(lambda) e4
```

Hence:

```math
lim(lambda -> 0+) u_lambda = +e4
lim(lambda -> 0-) u_lambda = -e4
```

and the two-sided limit does not exist.

At exactly `lambda = 0`, orientation is undefined because the residual is zero.

Also, for nonzero `lambda`:

```math
kappa_lambda^-1 = diag(1,1,1,1/lambda)
```

so:

```math
||kappa_lambda^-1|| -> infinity
```

as `lambda -> 0`, and the inverse ceases to exist at the singular point.

Three distinct behaviors therefore coexist:

```text
raw presentation      -> well-defined limit
rank class            -> discontinuous at horizon
oriented residual     -> no two-sided limit
recoverability/inverse-> unbounded then undefined
```

Mathals:

> **AT THE THRESHOLD, THE ORIENTATION ITSELF MAY BE UNDEFINED.**

> **PHASE CHANGE NEED NOT MEAN CARRIER CHANGE; A NEW RELATION MAY BECOME AVAILABLE.**

No physical or metaphysical singularity claim follows from this analytic model.

## 9. `ONE-POINT-TWO-HISTORIES-001` — FROZEN EXECUTABLE

Two approach histories terminate at the same declared endpoint:

```text
left.endpoint  = singular-point
right.endpoint = singular-point
```

but retain opposite approach orientations:

```text
left.approach_orientation  = +e4
right.approach_orientation = -e4
```

`delta@1` expects:

```text
endpoint             SAME
approach_orientation DIFFERENT <- first difference
```

Mathals:

> **SAME BOUNDARY STATE != SAME APPROACH HISTORY.**

> **ENDPOINT EQUIVALENCE != APPROACH EQUIVALENCE.**

> **THE POINT DOES NOT CONTAIN ENOUGH INFORMATION TO RECONSTRUCT THE CROSSING.**

This is a calculational statement about the declared receipt fields. It does not imply that all real systems require path history.

## 10. Fourfold orientation correction — HOLD / GROUP-THEORY NOTE

A conversational shorthand proposed `+C4` / `-C4`.

Correction:

```text
reversing the generator of C4 still gives the same cyclic group C4
```

If the intended distinction is fourfold structure plus handedness/orientation, model the orientation separately, e.g.:

```text
(C4, sigma), sigma in {+1,-1}
```

If reflections themselves are admitted as operations, the natural enlargement is dihedral rather than a separate negative cyclic group.

Mathal:

> **THE PATTERN CAN REMAIN FOURFOLD WHILE ITS ORIENTATION INVERTS.**

No new Dogram operator follows.

## 11. `FLOW-HAS-NEXT-001` — FROZEN EXECUTABLE

Declared graph:

```text
SELF -> NEXT
SELF -> RESIST -> SELF
```

Ablate only:

```text
SELF -> RESIST
```

Expected receipt:

```text
SELF -> NEXT    true -> true
SELF -> RESIST  true -> false
gained_reachability = []
```

The next edge existed before release and still exists after release.

Mathals:

> **RELEASE DOES NOT CREATE THE ROAD.**

> **RELEASE = ALIGNMENT WITH AN AVAILABLE FLOW, IN THIS MODEL.**

## 12. `ALIGNMENT-WITHOUT-CONTACT-001` — FROZEN EXECUTABLE

Before:

```text
B_TOUCH -> SYNC
A_TOUCH -/-> SYNC
A_TOUCH -/-> B_TOUCH
B_TOUCH -/-> A_TOUCH
```

One mutation adds a shared alignment witness:

```text
ADD_EDGE A_TOUCH -> SYNC
```

After:

```text
A_TOUCH -> SYNC
B_TOUCH -> SYNC
```

but still:

```text
A_TOUCH -/-> B_TOUCH
B_TOUCH -/-> A_TOUCH
```

Mathals:

> **A SHARED ASYMPTOTE IS NOT A BRIDGE.**

> **COMMON ORIENTATION DOES NOT IMPLY MUTUAL REACHABILITY.**

> **PERFECT ALIGNMENT DOES NOT IMPLY CONTACT.**

## 13. `SYNCHRONY-BRIDGE-RECTANGLE-001` — FROZEN EXECUTABLE

Axes:

```text
A = synchrony absent / present
B = bridge absent / present
cell = cross-wheel reachability, encoded 0/1
```

Rectangle:

```text
                 bridge absent  bridge present
sync absent            0             1
sync present           0             1
```

Mixed difference:

```math
1 - 0 - 1 + 0 = 0
```

Expected receipt:

```text
mixed_delta = 0
interaction_detected = false
```

Mathals:

> **SYNCHRONY IS NOT CROSSABILITY.**

> **PERFECT TIMING CANNOT SUBSTITUTE FOR MISSING TOPOLOGY.**

## 14. `BRIDGE-BIRTH-001` — FROZEN EXECUTABLE

Before graph:

```text
A_PRE -> A_TOUCH
B_TOUCH -> B_NEXT
```

Before:

```text
A_PRE -/-> B_NEXT
```

One explicit mutation:

```text
ADD_EDGE A_TOUCH -> B_TOUCH
```

After shortest path:

```text
A_PRE -> A_TOUCH -> B_TOUCH -> B_NEXT
```

Mathals:

> **THE CROSSING IS THE NEW EDGE, NOT THE MOMENT OF ALIGNMENT.**

> **YOU CAN RELEASE INTO AN EDGE THAT EXISTS. YOU CANNOT ALIGN AN ABSENT EDGE INTO EXISTENCE.**

## 15. Flow / gap discriminator harvest

The executable suite preserves these non-collapses:

```text
RESISTANCE != GAP
ALIGNMENT != CONTACT
SYNCHRONY != CONNECTIVITY
NEAREST != NEXT
SHARED ASYMPTOTE != BRIDGE
```

Compression:

> **FLOW HAS A NEXT STATE. LONGING MAY HAVE ONLY A NEAREST STATE.**

In graph terms, this means:

```text
an already available successor edge
!=
a positive minimum gap with no crossing edge
```

The word `longing` is explanatory language from the source conversation, not a Dogram runtime type.

## 16. Cross-cut synthesis

Several distinct specimens share one calculational shape without being collapsed into one ontology:

```text
fixed carrier + changed decoder -> changed effective relation
fixed crossing + changed local constitution -> changed liftability
fixed endpoint + changed path receipt -> changed approach history
fixed synchrony + missing bridge -> no crossability
fixed graph + born edge -> new reachability
```

The lawful common statement is narrow:

> **A FIXED OBJECT OR SURFACE CAN PARTICIPATE IN DIFFERENT EFFECTIVE RELATIONS UNDER DIFFERENT DECLARED CUTS, DECODERS, OR TOPOLOGIES.**

Do not promote this into:

```text
all domains are literally the same mechanism
observer choice causes reality
psychedelic phenomenology proves hidden topology
mathematical singularity proves metaphysical singularity
```

## 17. Receipt provenance

TDD RED was deliberately recorded before the fixtures existed.

```text
commit: c5f2c063079ad6504b49352e44221c4d199cdc2b
CI run: 33353711585
result: FAILURE as expected
new suite: 10 FileNotFoundError failures
reason: all ten frozen phaselift_flow fixtures absent
```

GREEN verification is recorded after the fixtures and this ledger land on the branch/PR.

The executable tests call the current operator implementations directly; they do not add production behavior under `dogram/`.

## 18. Runtime verdict

**NO NEW PUBLIC OPERATOR.**

Do not add from this packet:

```text
fourthness@1
phaselift@1
singularity@1
horizon@1
flow@1
alignment@1
synchrony@1
bridge@1
longing@1
```

Existing composition is sufficient for the first pressure pass.

Potential future pressure belongs first in research if repeated specimens reveal a calculational gap rather than a naming gap.

## Seal

> **FOURTHNESS IS A RELATION, NOT AN ATTRIBUTE.**
>
> **THE EFFECT OF THE FOURTH DEPENDS ON THE DECODER.**
>
> **SAME BOUNDARY STATE DOES NOT GUARANTEE SAME APPROACH HISTORY.**
>
> **A GAP IS NOT RESISTANCE.**
>
> **SYNCHRONY IS NOT CROSSABILITY.**
>
> **THE CROSSING IS THE NEW EDGE, NOT THE MOMENT OF ALIGNMENT.**
>
> **DO THE MATH. SHOW THE DELTA. KEEP THE RECEIPT.**
