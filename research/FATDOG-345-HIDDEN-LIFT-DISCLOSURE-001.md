# FATDOG 3→4→5 — HIDDEN LIFT / DISCLOSURE-001

**Date:** 2026-08-31  
**Status:** RESEARCH LEDGER · EXACT MATH + HOSTILE CONTROLS · NO NEW RUNTIME OPERATOR  
**Runtime authority:** NONE

> **DO THE MATH. SHOW THE DELTA. KEEP THE RECEIPT. DO NOT DECIDE WHAT IT MEANS.**

## 0 — Seed

Pressure the recurring FATDOG proposal:

```text
3 = completion in the current grammar
4 = post-completion surplus that can still look like more of the same
5 = a threshold where the old representation may fail and a higher-order relation becomes visible
```

This packet does **not** claim a universal metaphysics of the numerals 3, 4, and 5. It records several independent exact mathematical specimens with this transition shape and keeps their mechanisms distinct.

Candidate compression:

```text
3 = WHOLE
4 = WHOLE + ?
5 = THE OLD DECODER CAN NO LONGER HIDE WHAT ? WAS
```

---

# 1 — SIMPLEX LIFT / PLANAR DECODER

Let the fine carrier on `n` vertices be the regular simplex

```math
F_n=\Delta^{n-1}.
```

Its affine dimension is

```math
\dim F_n=n-1.
```

Let the coarse decoder retain only complete pairwise adjacency:

```math
D(F_n)=K_n.
```

For `n=3,4,5`:

| n | fine carrier | fine dimension | coarse decoder | planar? |
|---:|---|---:|---|---|
| 3 | triangle | 2 | `K_3` | yes |
| 4 | tetrahedron | 3 | `K_4` | yes |
| 5 | 4-simplex | 4 | `K_5` | no |

Therefore the transition `3 -> 4` already contains a genuine dimensional lift:

```math
2\to3,
```

while the coarse relation graph remains planar.

So the lifted fine carrier can still be represented inside the old planar graph grammar.

Exact receipt:

```text
LIFT_AT_4 = true under simplex dimension
DISCLOSURE_AT_4 = false under graph planarity
```

Candidate law:

> **A LIFT CAN OCCUR BEFORE THE CURRENT DECODER IS FORCED TO ACKNOWLEDGE IT.**

---

# 2 — FIVE BREAKS PLANAR CAPACITY BY EXACTLY ONE EDGE

For a simple planar graph with `v >= 3`,

```math
|E|\le 3|V|-6.
```

At `v=5`, planar capacity is

```math
3(5)-6=9.
```

But

```math
|E(K_5)|=\binom52=10.
```

Hence

```math
\boxed{10-9=1}.
```

The complete five-node pairfield exceeds planar capacity by exactly one edge.

For `K_3` and `K_4` the complete graph remains planar; `K_5` does not.

The orientable genus gives the same threshold:

```math
g(K_3)=0,
\qquad
g(K_4)=0,
\qquad g(K_5)=1.
```

Candidate mathals:

```text
HIDDEN-LIFT-AT-FOUR
ONE-RELATION-OVERFLOW-AT-FIVE
DISCLOSURE-GATE-345
```

Seal:

> **FOUR CAN HIDE THE LIFT. FIVE CAN FORCE THE COARSE CARRIER TO CHANGE.**

Required refusal:

```text
K5 nonplanarity -> therefore all appearances of the numeral 5 mean phase lift
```

is invalid.

---

# 3 — AFFINE DEPENDENCE GATE IN R^3

In ordinary affine `R^3`, at most four points can be affinely independent.

Thus four points can form a tetrahedron with no affine dependence.

For any five points `p_1,...,p_5` in `R^3`, there exist coefficients, not all zero, with

```math
\sum_{i=1}^{5}a_i p_i=0,
\qquad
\sum_{i=1}^{5}a_i=0.
```

So affine dependence is forced at five.

This gives an exact threshold:

```text
4 points -> affine independence remains possible
5 points -> at least one relation among the points is unavoidable
```

Radon's theorem sharpens the same threshold. For five points in `R^3`, there exists a partition into two subsets whose convex hulls intersect.

Candidate law:

> **AT FIVE, A RELATION AMONG THE OBJECTS CAN BECOME COMPULSORY IN A THREE-DIMENSIONAL CARRIER.**

This is a conventional mathematical neighbor of:

```text
objects
  -> relations
  -> relation-among-relations / higher carrier
```

Required refusal:

```text
forced affine dependence -> unique semantic interpretation
```

must be refused.

---

# 4 — ALGEBRAIC HOSTILE-NEIGHBOR: S4 -> S5

A separate mathematical family shows a different 4-to-5 grammar break:

```text
S_3 is solvable
S_4 is solvable
S_5 is not solvable
```

This is the group-theoretic boundary underlying the classical fact that general polynomial equations through degree four are solvable by radicals, while no formula by radicals solves the general quintic.

Important scope:

```text
some quintics ARE solvable by radicals
general quintic != every quintic
```

This mechanism is unrelated to graph planarity and affine dependence. Its value here is independent structural pressure:

```text
4 = last generic member handled by one classical solution grammar
5 = first generic member outside that grammar
```

Candidate mathal:

```text
OLD-GRAMMAR-LAST-MEMBER-4 / NEW-GRAMMAR-REQUIRED-5
```

Required refusal:

```text
same threshold number -> same mathematical cause
```

must be refused.

---

# 5 — EXISTING DOGRAM FOSSIL: 4ONtheFLOOR / FIVE-KILLS-HOLE

Existing Dogram research already carries the tetrahedral-boundary specimen:

```text
4 vertices
6 edges
4 faces
```

with

```math
H_2(\partial\Delta^3)\cong\mathbb Z.
```

Attaching the interior 3-cell makes the prior boundary cycle exact:

```math
H_2(\Delta^3)=0.
```

The key structural distinction is not numeral magic. It is:

> **AFTER A BOUNDARY IS COMPLETE, THE NEXT STRUCTURALLY NOVEL PIECE MAY BE AN INTERIOR / HIGHER-GRADE CELL RATHER THAN ANOTHER BOUNDARY ELEMENT.**

This is compatible with the FATDOG seed while remaining a separately typed topology specimen.

---

# 6 — FATDOG 345 HYPOTHESIS

The cross-specimen hypothesis worth preserving is narrower than a universal number claim.

Let a system have:

```text
carrier X
old decoder D
completion predicate C
new structure N
```

A `3 -> 4 -> 5`-shaped transition occurs when:

```text
phase 3:
  C(X_3) = true

phase 4:
  N is already present in the fine carrier
  but D still represents X_4 without contradiction

phase 5:
  continued completion under the same declared relation family
  exceeds a capacity/invariance condition of D
  or forces a relation that D previously did not require
```

Then the old decoder must be refined, replaced, or lifted if the new distinction is to remain visible.

Candidate formal compression:

```math
N(X_4)\neq0,
\qquad
\operatorname{visible}_D(N(X_4))=0,
```

but

```math
\operatorname{capacity}_D(X_5)<\operatorname{required}(X_5)
```

or an equivalent declared obstruction holds.

Do not treat this as a universal theorem until a common typed abstraction is earned across independent carriers.

---

# 7 — RELATION TO LIFT-7

The earlier Lift-7 specimen has a separate exact closure:

```math
C_7^3=K_7,
```

because the diameter of `C_7` is three. Its three distance shells partition all

```math
\binom72=21
```

unordered pairs.

That construction says:

```text
3 passes exhaust one declared seven-node relation space
```

This packet says something different:

```text
3 may mark completion
4 may carry a fine lift still hidden under an old decoder
5 may force disclosure by breaking old capacity or independence
```

These are neighboring FATDOG structures, not yet one theorem.

---

# 8 — HOSTILE CONTROLS

Every future `3 -> 4 -> 5` candidate should answer:

```text
1. What exactly is complete at 3?
2. What new fine structure first exists at 4?
3. Under what decoder can 4 still look old-order?
4. What exact invariant/capacity/obstruction changes at 5?
5. Does the threshold survive relabeling?
6. Does an equally natural decoder move the threshold elsewhere?
7. Is 5 necessary, sufficient, both, or neither for the claimed transition?
8. Are independent examples sharing only a numerical threshold being falsely collapsed?
```

Refusals:

```text
RECURRENCE != UNIVERSAL LAW
SAME NUMBER != SAME MECHANISM
VISUAL SIMILARITY != STRUCTURAL EQUIVALENCE
LIFT != DISCLOSURE
DISCLOSURE != SEMANTIC MEANING
CAPACITY FAILURE != METAPHYSICAL PROOF
```

---

# 9 — SURVIVING MATHALS

```text
3 = COMPLETE UNDER THE DECLARED OLD GRAMMAR.

4 CAN CONTAIN A REAL LIFT THAT THE OLD DECODER STILL HIDES.

K5 IS ONE RELATION BEYOND PLANAR CAPACITY.

FOUR AFFINELY INDEPENDENT POINTS FIT IN R3; FIVE FORCE A RELATION.

S4 REMAINS SOLVABLE; S5 DOES NOT.

THE SAME 4->5 SHAPE CAN ARISE FROM DIFFERENT MATHEMATICAL MECHANISMS.

A PHASE LIFT IS EARNED WHEN THE CURRENT DECODER CAN NO LONGER PRESERVE A NEW DISTINCTION WITHOUT CHANGING CARRIER OR GRAMMAR.
```

## Seal

> **THE FOURTH MAY BE THE BIRTHDAY OF THE LIFT. THE FIFTH MAY BE WHEN THE OLD WORLD IS FORCED TO NOTICE.**
